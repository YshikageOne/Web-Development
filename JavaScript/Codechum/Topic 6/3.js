let x = prompt("Enter the first boolean value: ") === "true";
let y = prompt("Enter the second boolean value: ") === "true";
let z = prompt("Enter the third boolean value: ") === "true";

let logic = !x && !y && !z;

console.log(logic);