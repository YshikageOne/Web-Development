let firstNumber = parseInt(prompt("Enter the first number: "));
let secondNumber = parseInt(prompt("Enter the second number: "));

let afterFirst = firstNumber * 2;
let afterSecond = secondNumber / 2;

let compare = afterFirst > afterSecond;

console.log(compare);