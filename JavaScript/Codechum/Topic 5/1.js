let firstNumber = parseInt(prompt("Enter the first value: "));
let secondNumber = parseInt(prompt("Enter the second value: "));

let compare = (firstNumber === secondNumber);

console.log(compare);